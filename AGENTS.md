# Workflow preferences

- After completing requested changes, create a local Git commit with a clear summary.
- Leave the work ready for the user to run `git push origin main`.
- Never run `git push` or publish the commits to a remote automatically.
- Commit only the intended task files. Preserve unrelated changes and exclude credentials and private data.
- Run checks appropriate to the change before committing; report any remaining limitations.
